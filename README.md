# BUlk Upload Workspace

This workspace is for BRD analysis, feature files, and test artifacts.

Structure:
- docs/        : project documentation
- brd/         : place BRD files (PDF/DOCX)
- features/    : Gherkin feature files
- tests/       : automated/manual test scripts
- artifacts/   : reports and outputs

Next steps:
1. Upload the BRD to `brd/` (copy PDF/DOCX into the folder).
2. I'll analyze the BRD and produce Gherkin and test cases in `features/` and `tests/`.
